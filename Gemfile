source 'https://rubygems.org'

gem 'calabash-cucumber', '~>0.19.0'
gem 'trollop', '~>2.1.2'
gem 'mockserver-client', '~>1.0.7'

# run_loop 2.1.0 has a bug that requires a manual work-around that we don't want to apply, hopefully they will fix it soon and we can remove the forced version below
# see https://github.com/calabash/run_loop/blob/develop/CHANGELOG.md#210
gem 'run_loop', '2.1.1'